<UserControl x:Class="BookMan.WPF.PeopleControl"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" 
             d:DesignWidth="640">
    <Grid>
        <DataGrid ItemsSource="{Binding}" AutoGenerateColumns="True" Height="166" HorizontalAlignment="Stretch" Name="dataGrid1" VerticalAlignment="Top" DataContext="{Binding}" />
        <TextBox Text="{Binding Path=Surname}" Height="23" HorizontalAlignment="Left" Margin="166,218,0,0" Name="textBox1" VerticalAlignment="Top" Width="120" />
    </Grid>
</UserControl>
